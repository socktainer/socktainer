[package]
name = "socktainer-port-relay"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "Apache-2.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"

