#!/bin/bash

set -e

echo "Glass Dock __VERSION__ installation completed successfully!"

# Verify the installation
if [ -f "__INSTALL_PREFIX__/bin/glassdock" ]; then
    echo "✅ glassdock binary installed to __INSTALL_PREFIX__/bin/glassdock"
    
    # Make sure it's executable
    chmod +x "__INSTALL_PREFIX__/bin/glassdock"
    
    # Add to system PATH when requested. Isolated QA packages can disable this.
    if [ "__INSTALL_PATHS_D__" = "1" ]; then
        if [ ! -d "/etc/paths.d" ]; then
            mkdir -p /etc/paths.d
        fi
        echo "__INSTALL_PREFIX__/bin" > "/etc/paths.d/__PATHS_D_NAME__"
        echo "✅ Added __INSTALL_PREFIX__/bin to system PATH"
    fi
    
    echo ""
    echo "🚀 Quick start:"
    echo "   Open a new terminal, then run:"
    echo "   glassdock --help      # Show available commands"
    echo "   glassdock --version   # Display version information"
    echo ""
    echo "   Or use the full path:"
    echo "   __INSTALL_PREFIX__/bin/glassdock --help"
    echo ""
    echo "Happy containerizing! 🍏📦🐳"
else
    echo "⚠️  Warning: glassdock binary not found at expected location"
    exit 1
fi
